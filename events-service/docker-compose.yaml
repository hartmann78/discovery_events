services:
  app:
    image: 'events-service-app:latest'
    build:
      context: .
    container_name: events-service
    depends_on:
      - events-service-db
      - rabbitmq
    ports:
      - "8080:8080"
    environment:
      - SPRING_APPLICATION_NAME=events_service
      - SPRING_DATASOURCE_URL=jdbc:postgresql://events-service-db:5432/events-service
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=postgres
      - SPRING_JPA_HIBERNATE_DLL_AUTO=update
      - SERVER_PORT=8080
      - STATS_SERVICE_HOST=host.docker.internal
      - STATS_SERVICE_PORT=9090
      - SPRING_RABBITMQ_HOST=rabbitmq
      - SPRING_RABBITMQ_PORT=5672
      - SPRING_RABBITMQ_USERNAME=guest
      - SPRING_RABBITMQ_PASSWORD=guest
    networks:
      - discovery-events-network
  events-service-db:
    image: postgres:17.4
    container_name: events-service-db
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=events-service
    networks:
      - discovery-events-network
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - discovery-events-network

networks:
  discovery-events-network:
    name: discovery-events-network
    driver: bridge